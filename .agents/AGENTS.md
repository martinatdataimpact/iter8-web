# iter8-web — Agent Rules

## ⚠️ CRITICAL SAFETY RULES

> [!CAUTION]
> **DO NOT DEPLOY THE LAUNCH SITE TO ITER8.SE!**
>
> You are **STRICTLY PROHIBITED** from deploying the "launch" site or any new launch material to `iter8-web` (which serves `iter8.se`) until **autumn 2026 at the earliest**.
>
> - The live site must remain the **public-web** version.
> - If you are asked to work on the launch site, do so in a separate branch or a separate directory (e.g. `manhattan-df/public-web/`), but **never commit it to the `main` branch of this repository (`iter8-web`)**.
> - Do not push `om.html`, `kontakt.html`, `produkter.html` or other launch-specific assets to this repository's `main` branch.
